# status/modules/sync.conf
# vim:set ft=tmux:
# Synchronize-panes indicator - color matches current mode

set -ogq @oasis_module_sync "\
#{?pane_synchronized,#{\
#[bg=#{@thm_mantle},fg=#{E:@oasis_color_mode}]\
#[bg=#{E:@oasis_color_mode},fg=#{@thm_surface},bold] SYNC \
},#{}}"
